import React from 'react';

import './styles.css';

const Header = () => {
  return (
    <header className="main-header-container">
      <h2 className="main-header-title">BDS Vendas</h2>
    </header>
  );
};

export default Header;
